# NPM Workspace Configuration
# Enable workspaces
workspaces=true

# Hoist dependencies to root node_modules
hoist-pattern[]=*

# Use exact versions for better consistency
save-exact=true

# Use npm cache for faster installs
cache-min=3600

# Prefer npm over yarn
prefer-npm=true

